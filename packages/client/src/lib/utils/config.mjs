/**
 * @typedef {import('next').NextConfig} NextConfig
 */

/**
 * @template {NextConfig} T
 * @param {T} options - Next.js configuration.
 * @returns {T}
 */
export function defineNextConfig(options) {
  return options;
}
